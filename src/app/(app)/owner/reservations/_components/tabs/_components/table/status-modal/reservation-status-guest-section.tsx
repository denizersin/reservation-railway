import { TReservationRow } from '@/lib/reservation'
import React from 'react'

type Props = {
    reservation: TReservationRow
}

export const ReservationStatusGuestSection = (props: Props) => {
    return (
        <div className=''>reservation-status-guest-section</div>
    )
}
