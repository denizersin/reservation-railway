import { TReservationRow } from '@/lib/reservation'
import React from 'react'

type Props = {
    reservation: TReservationRow
}

export const ReservationStatusActions = (props: Props) => {
    return (
        <div>ReservationStatusActions</div>
  )
}