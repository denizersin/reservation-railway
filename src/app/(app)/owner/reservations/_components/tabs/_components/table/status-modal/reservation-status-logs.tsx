import { TReservationRow } from '@/lib/reservation'
import React from 'react'

type Props = {
    reservation: TReservationRow
}

export const ReservationStatusLogs = (props: Props) => {
    return (
        <div>ReservationStatusLogs</div>
    )
}
