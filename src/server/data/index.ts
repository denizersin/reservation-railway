import { predefinedData } from "./db/predefined";
import { reviewCategories } from "./review";
import { reservationTags } from "./tag";

export const serverData = {
    predefinedData,
    reviewCategories,
    reservationTags
}
